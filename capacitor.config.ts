import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bubble-scroll-test',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
